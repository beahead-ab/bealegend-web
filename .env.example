# Local development only. In production the web is served from the same origin
# as the API (Caddy proxies /api/*), which is what makes the session cookie a
# first-party cookie — see docs/MVP_PLAN.md §1.
VITE_API_URL=http://localhost:8080
